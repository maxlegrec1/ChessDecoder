[build-system]
requires = ["setuptools>=68", "wheel", "pybind11>=2.12"]
build-backend = "setuptools.build_meta"

[project]
name = "cutlass_decoder_cpp"
version = "0.1.0"
description = "CUTLASS-based ChessDecoder inference engine (FP16 + FP8)"
requires-python = ">=3.10"
