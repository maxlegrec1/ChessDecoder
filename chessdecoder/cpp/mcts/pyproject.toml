[build-system]
requires = ["setuptools>=64", "pybind11>=2.12"]
build-backend = "setuptools.build_meta"

[project]
name = "chessrl-inference-cpp"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["pybind11>=2.12"]

[tool.setuptools]
py-modules = []
packages = []
